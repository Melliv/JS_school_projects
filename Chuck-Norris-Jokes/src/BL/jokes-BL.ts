import { IJokesInfo } from "../domain/IJokesInfo";
import { JokesState } from "../state/jokes-state";

export class JokesBl {
    constructor() {
    }

    async addInitialInfo(pageNum: number): Promise<void> {

    }
}